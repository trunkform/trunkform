# GITHUB OIDC Role

```sh
aws cloudformation deploy --region us-east-2 --stack-name github-oidc-provider-trunkform --template-file cfn.yml --no-fail-on-empty-changeset --capabilities CAPABILITY_NAMED_IAM
```
